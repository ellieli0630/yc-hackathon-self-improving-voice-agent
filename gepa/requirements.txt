openai>=1.40.0
httpx>=0.27.0
pydantic>=2.0

# GEPA prompt optimizer (dspy_gepa.py); dspy pulls in the `gepa` PyPI package.
dspy>=3.2.1
# Golden-set build (build_golden_set.py): Upstash vector recall + S3 upload.
upstash-vector>=0.8.0
boto3>=1.34.0
