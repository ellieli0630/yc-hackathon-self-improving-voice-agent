pipecat-ai[daily,cartesia,nvidia,silero]>=1.3,<2

# Daily Python SDK (pulled in by pipecat[daily] but pinned)
daily-python>=0.10.0

# Cekura tracing SDK for Pipecat
cekura[pipecat]==1.2.0

# HTTP client for tool callbacks to Nomie API
httpx>=0.27.0

pydantic>=2.0
python-dotenv>=1.0.0
loguru>=0.7.0

fastapi>=0.110.0
uvicorn>=0.27.0
